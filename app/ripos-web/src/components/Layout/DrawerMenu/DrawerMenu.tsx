import { Drawer } from '@mui/material';

const DrawerMenu = () => {
  return (
    <Drawer variant="permanent" open={true}>
      Drawer menu
    </Drawer>
  );
};

export default DrawerMenu;
