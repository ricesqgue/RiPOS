import { createTheme } from '@mui/material';

const palette = {};

const theme = createTheme({
  palette: palette,
  typography: {
    fontFamily: 'Ubuntu',
  },
});

export default theme;
